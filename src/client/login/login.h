// Definations for all parser functions
#ifndef CLIENT_LOGIN
#define CLIENT_LOGIN

// Performs login
int login(char * preference, char * name, char * username, char * password);

#endif // CLIENT_LOGIN
